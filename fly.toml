app = "tg-ai-bot"

[build]
  dockerfile = "Dockerfile"

[env]
  GEMINI_MODEL = "gemini-3.5-flash"
  MAX_HISTORY = "20"

[[services]]
  internal_port = 8080
  protocol = "tcp"

  [services.concurrency]
    type = "connections"
    hard_limit = 25
    soft_limit = 20
